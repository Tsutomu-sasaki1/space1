
<h1>ユーザー投稿一覧</h1>
      <% @post.each do|post|%>
        <li class='list'>
          <%# <%= link_to user_path(post.id) do %>
            <%#<div class='item-img-content'>
              <%= image_tag(item.image, class: "item-img") %>
           <%# </div>
          <% end %> %>
        </li>
      <% end %>